#include <cs50.h>
#include <stdio.h>
#include <ctype.h>
#include <string.h>

int main(void)
{
    
}

string validate(string card_num)
{

}

string process(string card_num)
{
    for (int i = 0; i < strlen(card_num); i++)
    {
        printf(cardnum[i]);
    }
}