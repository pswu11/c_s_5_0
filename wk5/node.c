

// initialize node to NULL to make sure there's no garbage value there.
node *list = NULL;
node *n = malloc(sizeof(node));
node *n = malloc(sizeof(node));

// (*n).number = 1;
n->number = 1;
n->next =

